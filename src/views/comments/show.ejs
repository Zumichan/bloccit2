<% if(comment) { %>
  <div style="width: 50%">
    <hr>
    <p>
      <%= comment.body %>
    </p>
    <small class="card-subtitle mb-2 text-muted">
      <!-- When we load the comments, we also load the associated user for each comment and load it to a property called User-->
      sent by <%= comment.User.email %> on <%= comment.createdAt %>
    </small>

    <!-- We will only display a delete button if the user is the owner or an admin-->
    <% if(currentUser && (currentUser.id === comment.userId || currentUser.isAdmin())) { %>
      <form style="display:initial"
            action="/topics/<%= post.topicId %>/posts/<%= post.id %>/comments/<%= comment.id %>/destroy"
            method="post">
        <button type="submit" class="btn btn btn-sm btn-outline-danger">Delete</button>
      </form>
    <% } %>
    <hr>
  </div>
<% } %>
